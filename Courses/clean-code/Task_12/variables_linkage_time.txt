1.
double min_dist = scene->min_dist_from_point(ray.get_position());

// В данном случае переменная связывается со своим значением во время исполнения.
// Более раннее связыване не возможно так как данное вычисление возможно только после запуска программы. 

2.
String walls = "$@B%8&WM#*oahkbdpqwmZO0QLCJUYXzcvunxrjft/\\|()1{}[]?-_+~<>i!lI;:,\"^`'.";

// Тут переменная связывается со значением во время компиляции.
// В более позднем связывании не было необходимости поскольку Создание всей необходимой функциональности было возможно без этого.

3.

SDL_Renderer *renderer = SDL_CreateRenderer(window, -1, SDL_RENDERER_ACCELERATED | SDL_RENDERER_PRESENTVSYNC);

// Тут связывание происходит единожды во время загрузки программы.
// Потому что более раннее связывание не возможно так как значение зависит от платформы.
// А боллее позднее связывание не требовалось.
